# Redis 端口
port 6379

# 持久化
dir /data
appendonly yes

# 设置超时时间（0 表示永不超时）
timeout 0

# TCP keepalive（防止网络设备断开空闲连接）
tcp-keepalive 300

# 设置最大连接数
maxclients 10000

#开放远程
bind 0.0.0.0
protected-mode no

# 密码
requirepass 123456

# 日志
logfile ""